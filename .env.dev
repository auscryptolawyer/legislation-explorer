# Dev environment — no auth, no rate limiting, no session secrets
DEV_MODE=true
LEGISLATION_BEARER_TOKEN=
JWT_SECRET=
AZURE_CLIENT_ID=
SESSION_SECRET=dev-insecure
ALLOWED_ORIGINS=https://dev.scriptkitty.yachts,http://localhost:5173,http://localhost:3000,http://localhost:8766
RATE_LIMIT_ENABLED=false